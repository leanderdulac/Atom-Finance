# Sample QuantMind Document

## Section One

This is the first paragraph of section one. It contains some prose that
might be normalized by `clean.normalize_unicode` (smart quotes "like
these" or 1/2 ligatures).

## Section Two

A second paragraph follows here. The fixture is intentionally short — it
exists to give future markdown helpers a known-good input without us
having to compose it inline in every test.
