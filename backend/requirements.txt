# Core Framework
fastapi>=0.135.0,<1
uvicorn[standard]~=0.30.6
pydantic~=2.9.2

# Authentication
PyJWT[crypto]>=2.12.0,<3
bcrypt~=4.2.0
python-multipart~=0.0.12

# Quantitative Finance
numpy~=1.26.4
scipy~=1.14.1
pandas~=2.2.3

# Data Sources
yfinance>=0.2.58
requests>=2.33.0,<3

# Machine Learning
scikit-learn~=1.5.2
torch>=2.13.0,<3
torchsde~=0.2.6
einops~=0.8.1
huggingface-hub~=0.33.1
tqdm~=4.67.1

# AI / LLM SDKs
anthropic>=0.40.0
openai>=1.50.0
google-genai>=1.0.0

# Reports
openpyxl>=3.1.5

# Utilities
python-dotenv>=1.2.2,<2
httpx~=0.27.2
redis~=5.1.1

# Postgres migration spike (spike/postgres-migration branch only — see
# docs/POSTGRES-MIGRATION-SPIKE.md). Alembic pulls in SQLAlchemy + greenlet
# for its own async engine; the app itself queries via asyncpg directly.
asyncpg~=0.30.0
alembic~=1.19.2
greenlet~=3.1.1

# Rate limiting
slowapi~=0.1.9

# CORS
starlette>=1.3.1,<2

# Testing
pytest~=9.0.2
pytest-asyncio~=1.3.0
pytest-cov~=6.0.0
